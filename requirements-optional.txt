# 可选能力：按需安装（亦可用 pip install -e ".[vector,legacy-db,dev]"）
# LangChain / LangGraph 已纳入 pyproject 主依赖（ReAct 智能体底层）。

# --- 本地向量嵌入（sentence-transformers / torch）---
torch>=2.0
sentence-transformers>=2.0

# --- 遗留/企业中间件连接（配置项存在，桌面默认可不装）---
mysql-connector-python>=8.0
redis>=5.0
elasticsearch>=8.0
pymilvus>=2.3
minio>=7.0
