原计划在执行中遇到失败或新情况，请根据已执行结果输出**剩余**应执行的步骤。

## 原始任务

{task}

## 原计划（含状态）

{original_plan}

## 已执行轨迹摘要

{trajectory}

## 输出要求

只输出一个 **JSON 数组**（不要其它说明）。格式与初次规划相同：每项含 `description`、`tool_name`、`tool_params`、`depends_on`（可空数组）。

仅包含尚未完成且仍需执行的步骤；不要重复已成功完成的步骤。
