# 推荐：可编辑安装（单一事实来源见 pyproject.toml）
#   pip install -e .
# 可选能力：
#   pip install -e ".[langchain,vector,legacy-db,dev]"
#
# 若仍使用纯 requirements 文件：
-r requirements-base.txt
# 需要时再取消下行注释：
# -r requirements-optional.txt
